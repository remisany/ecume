export interface IColors {
    yellow: string,
    orange: string,
    background: string,
    white: string
}

export interface ISize {
    fontTitle: number,
    fontSize: number,
    radius: number
}

export interface IStyleConstants {
    colors: IColors,
    size: ISize
}
